# Local SQLite defaults
DATABASE_PATH=apps/backend/dev.db
JWT_SECRET=change-this-local-development-secret
PORT=4000
CORS_ORIGIN=http://localhost:4000

# Production backend database (Render/Railway/Neon/Supabase Postgres)
# DATABASE_URL=postgresql://user:password@host:5432/kebab_events

# Frontend runtime config lives in apps/frontend/config.js.
# Set API_BASE_URL there to your deployed backend origin for Vercel.
